package models.formdata

case class EventPropertyForm(
                        id: Option[String],
                        name: String,
                        order: Int
                       )
{ }
